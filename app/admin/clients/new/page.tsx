import AdminShell from '@/components/AdminShell'; import NewClientForm from '@/components/NewClientForm'; import {requireAdminPage} from '@/lib/auth';
export default async function NewClient(){await requireAdminPage();return <AdminShell><div className="pageHead"><div><div className="eyebrow">NEW CLIENT INTELLIGENCE</div><h1>Create Client Workspace</h1><p>Enter the minimum business information. The system will use it as the starting point for research, branding and proposal preparation.</p></div></div><NewClientForm/></AdminShell>}
