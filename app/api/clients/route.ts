import {NextResponse} from 'next/server';import crypto from 'crypto';import {admin} from '../../../lib/supabase';
export async function POST(req:Request){const b=await req.json(),db=admin(),token=crypto.randomBytes(18).toString('base64url');const {data,error}=await db.from('clients').insert({business_name:b.business_name,first_name:b.first_name,last_name:b.last_name,email:b.email,website:b.website,industry:b.industry,start_date:b.start_date||null,logo_url:b.logo_url||null,primary_color:b.primary_color,accent_color:b.accent_color,status:'intake_sent'}).select().single();if(error)return NextResponse.json({error:error.message},{status:400});await db.from('intake_links').insert({client_id:data.id,token,status:'active'});return NextResponse.json({id:data.id,url:`/intake/${token}`})}
