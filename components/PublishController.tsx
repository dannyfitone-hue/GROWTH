'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function PublishController({clientId,published}:{clientId:string,published:boolean}){const [busy,setBusy]=useState(false);const [err,setErr]=useState('');const router=useRouter();async function go(){setBusy(true);setErr('');const r=await fetch(`/api/clients/${clientId}/publish`,{method:'POST'});const j=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setErr(j.error||'Publish failed');return;}router.refresh();}return <div>{published?<div className="success">Client portal is published and ready to send.</div>:<button className="btn gold" disabled={busy} onClick={go}>{busy?'Publishing…':'Approve & Publish Client Portal'}</button>}{err&&<div className="error" style={{marginTop:10}}>{err}</div>}</div>}
